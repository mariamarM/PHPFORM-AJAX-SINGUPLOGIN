- api.php ya no devuelve el "ok":true, ahora solo devuelve el array donde se gurdan los usuarios
- le puse el editar a los usuarios solo los unicos campos que hay de la tabla y encima con modal.
- los codigos de errores los uso del httpo q es la propiedad que tiene el fetch para saber si todas las respuestas fluyen como el rio bravo
- solo se duplican nombres, los correos actuan como pk pero no es lo mejor aunque no es presncidible de la practica
  2n BRANCH
  - gestion usuarios por roles, admin y user
  - restricciones de pestañas
  - usuarios solo formulario y administrador panel usando ajax
